# CentOS 7.9（x86_64）：5.x 内核 RPM 获取/构建思路笔记

目标：在 **CentOS 7.9 x86_64** 上获得可安装的 **5.x（优先 5.4 LTS）内核 RPM**，并以“可追溯/可维护”的方式交付（自签名 + 自建仓库）。

---

## 总结结论

- **优先方案：使用 ELRepo 的 spec/config 作为打包蓝图，自行在 EL7 环境构建 x86_64 RPM**（可控、可持续）。
- **Buildlogs Mirror 方案仅作为“历史产物/线索存档”**：可用于对照版本、拉 SRPM 参考，但不建议直接当生产 yum 源。

---

## 方案 A（优先）：基于 ELRepo 的 GitHub 仓库自行构建（推荐）

仓库：`https://github.com/elrepo/kernel`

### 适用性判断

- 该仓库提供 `kernel-lt` / `kernel-ml` 的 spec 与配置（按 `el7/el8/el9/...` 分目录），目标就是产出可安装的 RPM。
- 对“只要 5.x”：
  - `kernel-lt` 里有 5.4 线（LTS），适合 CentOS 7.9 这种老用户态环境。

### 推荐的交付形态（生产）

- 在 EL7 构建环境内构建出一组 RPM（至少 `kernel-lt`；常见还需要 `kernel-lt-devel`/`kernel-lt-headers`/`kernel-lt-tools` 视需求）。
- 将 RPM 放入自建仓库目录并生成 `repodata`（`createrepo_c`），并使用你自己的 GPG key 对仓库/RPM 签名（形成稳定信任链）。
- 在 CentOS 7.9 目标机上通过自建 repo 安装与升级（版本冻结、回滚更可控）。

### 构建环境建议

- 使用容器或 VM 启一个 EL7 构建环境（例如 `mock` 或 “干净的 CentOS 7 chroot/容器”）。
- 构建尽量在隔离环境里完成；产物通过挂载目录/共享目录导出到你的交付介质（内部仓库/制品库/离线介质等）。

### 关键风险点（提前认知）

- EL7 默认编译链较老：遇到编译工具版本不足时，通常需要按 spec 提示引入更新的 toolchain（或调整构建选项）。
- 模块/驱动生态：5.4 内核可能影响现有 kmod、DKMS、闭源驱动；需要提前验证（尤其是网卡/存储/HBA/RAID）。
- 版本维护：你自己接手了“何时更新到下一个 5.4.y、何时回补安全修复、如何回滚”的责任。

---

## 方案 B：CentOS Buildlogs Mirror（仅存档/线索，不建议直接生产）

入口索引：`https://buildlogs.centos.org/c7-kernels.x86_64/kernel/`

### 可以从中得到什么

- 历史构建产物与目录时间戳，可用于：
  - 对照“某个版本曾在 CentOS 构建系统里出现过”
  - 获取 SRPM 作为重建参考（更建议拿 SRPM 自建构建链）

### 已确认的关键信息（仅 x86_64 视角）

- 最新构建目录时间戳可见 **2023-01-02**（`20230102143954`）：
  - `https://buildlogs.centos.org/c7-kernels.x86_64/kernel/20230102143954/`
- 该目录下出现 `5.4.225-200.el7`，但 **未提供 `x86_64/`**（仅 `i386/` + `noarch/`），因此 **不能作为 “CentOS 7.9 x86_64 直接可用的 5.4.225 RPM 来源”**。
- 该树里存在较早的 `x86_64` 示例：`20210111160234` 下可见 `5.4.88-200.el7.x86_64`：
  - `https://buildlogs.centos.org/c7-kernels.x86_64/kernel/20210111160234/`

### 为什么不建议直接用于生产

- 它是构建留档/镜像，不等同于正式发行仓库：常见问题是签名/信任链、仓库自洽性、持续更新承诺不足。
- 生产更推荐：把 SRPM 拉下来 → 在你自己的 EL7 构建环境重建 → 自签名 → 自建仓库后部署。

---

## 版本选择建议（只谈 5.x）

- 如果你只是想要“5.x 且稳定可维护”：优先 **5.4 LTS**（`kernel-lt` 路线）。
- 如果你更在意“更高版本新特性”：那通常会超出本文 5.x 范畴（但 ELRepo 同仓���也有 `kernel-lt 6.1` / `kernel-ml` 等线）。

---

## 最小落地清单（行动项）

- 选定内核线：`kernel-lt 5.4.y`。
- 在 EL7 构建环境完成 RPM 构建（x86_64）。
- 自签名 + 生成 `repodata`，形成内部 yum 源。
- 在 CentOS 7.9 x86_64 测试机验证：启动、网卡/存储、常用内核模块、以及你依赖的 DKMS/kmod。

